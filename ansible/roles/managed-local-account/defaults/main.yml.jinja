---
# Defaults for managed-local-account ({{ role_name }}).
role_enabled: true
managed_local_account_name: "{{ role_name }}_svc"
managed_local_account_comment: "Service account managed by {{ role_name }}"
