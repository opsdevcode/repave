services:
  repave:
    build:
      context: ../..
      dockerfile: deploy/local/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ../..:/app
    working_dir: /app
    command: ["repave", "serve", "--repo-root", "/app", "--host", "0.0.0.0", "--port", "8080"]
