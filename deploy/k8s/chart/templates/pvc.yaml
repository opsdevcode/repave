{{- if and .Values.persistence.modules.enabled (not .Values.persistence.modules.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "repave.fullname" . }}-modules
  labels:
    {{- include "repave.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.modules.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.modules.size }}
  {{- if .Values.persistence.modules.storageClass }}
  storageClassName: {{ .Values.persistence.modules.storageClass | quote }}
  {{- end }}
{{- end }}
---
{{- if and .Values.repave.audit.enabled .Values.persistence.audit.enabled (not .Values.persistence.audit.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "repave.fullname" . }}-audit
  labels:
    {{- include "repave.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.audit.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.audit.size }}
  {{- if .Values.persistence.audit.storageClass }}
  storageClassName: {{ .Values.persistence.audit.storageClass | quote }}
  {{- end }}
{{- end }}
---
{{- if and .Values.repave.fleet.enabled .Values.persistence.fleet.enabled (not .Values.persistence.fleet.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "repave.fullname" . }}-fleet
  labels:
    {{- include "repave.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.fleet.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.fleet.size }}
  {{- if .Values.persistence.fleet.storageClass }}
  storageClassName: {{ .Values.persistence.fleet.storageClass | quote }}
  {{- end }}
{{- end }}
---
{{- if and .Values.repave.durability.enabled .Values.persistence.runs.enabled (not .Values.persistence.runs.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "repave.fullname" . }}-runs
  labels:
    {{- include "repave.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.runs.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.runs.size }}
  {{- if .Values.persistence.runs.storageClass }}
  storageClassName: {{ .Values.persistence.runs.storageClass | quote }}
  {{- end }}
{{- end }}
---
{{- if and (include "repave.environmentVendingEnabled" .) .Values.persistence.environments.enabled (not .Values.persistence.environments.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "repave.fullname" . }}-environments
  labels:
    {{- include "repave.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.environments.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.environments.size }}
  {{- if .Values.persistence.environments.storageClass }}
  storageClassName: {{ .Values.persistence.environments.storageClass | quote }}
  {{- end }}
{{- end }}
