# repave-cli (extraction target)

Future home of `repave-tf` (`cli/`). Already a separate Python package with import-boundary tests
([`cli/tests/test_boundary.py`](../../cli/tests/test_boundary.py)).

## Extract

```bash
./scripts/extract-repos/extract-cli.sh /path/to/repave-cli.git
```
