# repave-operator (extraction target)

Future home of the Kubernetes operator (`operator/`). Already an isolated Go module with its own
CI ([`.github/workflows/operator.yml`](../../.github/workflows/operator.yml)).

Published as `ghcr.io/opsdevcode/repave-operator`.

## Extract

```bash
./scripts/extract-repos/extract-operator.sh /path/to/repave-operator.git
```
