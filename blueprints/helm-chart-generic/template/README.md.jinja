# {{ chart_name }}

{{ description }}

Generated by repave blueprint `helm-chart-generic`.

## Usage

Install or upgrade the chart:

```bash
helm upgrade --install {{ chart_name }} . --namespace default
```

Render templates locally:

```bash
helm template {{ chart_name }} .
```

Lint the chart:

```bash
helm lint .
```

## Configuration

See `values.yaml` for `image`, `service`, and optional `ingress` settings.

## Provenance

Lineage is recorded in `repave.yaml` when you publish. Re-run generate to refresh pins.
