# {{ project_name }}

{{ description }}

Generated by repave blueprint `ansible-playbook-project`.

## Project layout

- `site.yml` — entry playbook (FQCN modules)
- `inventories/{{ environment }}/` — primary scaffolded environment
- `requirements.yml` — pin Galaxy roles and collections
- `group_vars/all/` — shared variables

## Standard

This project was generated against repave standard `{{ _repave_standard_source }}`
v{{ _repave_standard_version }} (playbook-project standard).

## Usage

```bash
ansible-galaxy install -r requirements.yml
ansible-playbook site.yml
```

Use `-i inventories/<env>/hosts.yml` when overriding the default in `ansible.cfg`.

## Development

```bash
yamllint .
ansible-lint
ansible-playbook --syntax-check site.yml
```

## Provenance

Generation metadata is in `repave.yaml`. The `provenance-drift` gate validates it on
every generate/publish.
