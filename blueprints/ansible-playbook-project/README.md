# ansible-playbook-project

Golden path for an **Ansible playbook project** repository (`ansible-playbook-*`) with
`site.yml`, pinned `requirements.yml`, environment inventory layout, and production-profile
ansible-lint.

Standard: `standards/ansible/playbook-project-standard.md` @ 1.0.0.
