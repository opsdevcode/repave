# ansible-collection-generic

Golden path for a Galaxy-compatible Ansible collection (`galaxy.yml`, `meta/runtime.yml`,
starter role under `roles/`). Pins `standards/ansible/collection-standard.md` v1.0.0.

Output repo name: `ansible-collection-{namespace}-{collection_name}`.
