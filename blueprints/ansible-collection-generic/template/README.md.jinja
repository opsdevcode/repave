# {{ namespace }}.{{ collection_name }}

{{ description }}

Generated by repave blueprint `ansible-collection-generic`.

## Standard

This collection targets `standards/ansible/collection-standard.md` v{{ _repave_standard_version }}.
Provenance is recorded in `repave.yaml`.

## Layout

- `galaxy.yml` — collection metadata for Galaxy publish
- `meta/runtime.yml` — Ansible core version requirement
- `roles/sample/` — starter role (extend or add roles)
- `changelogs/changelog.yaml` — collection changelog scaffold

## Development

```bash
ansible-lint
yamllint .
```

## Provenance

Generation metadata is in `repave.yaml`. The `provenance-drift` gate validates it on
every generate/publish.
