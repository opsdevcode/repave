# azure-policy-generic

Golden path for **`azure-policy`** artifacts: Azure Policy definition JSON under
`policy/definitions/`.

Output repo name: `azure-policy-{organization}-{policy_name}`.

Standards: `standards/policy/azure.md` and `standards/policy/governance-baseline.md`.
