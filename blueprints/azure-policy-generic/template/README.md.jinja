# {{ policy_name }}

{{ description }}

Generated by repave blueprint `azure-policy-generic`.

## Standard

This repository follows `standards/policy/azure.md` v{{ _repave_standard_version }} and the
shared governance baseline in `standards/policy/governance-baseline.md`.

## Usage

Add Azure Policy definition JSON files under `policy/definitions/`. The `azure-policy` gate
validates required fields (`displayName`, `policyType`, `mode`, `description`, `policyRule`)
at generate time.

## Provenance

Generation metadata is in `repave.yaml`. The `provenance-drift` gate validates it on
every generate/publish.
