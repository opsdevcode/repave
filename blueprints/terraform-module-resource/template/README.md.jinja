# {{ module_name }}

{{ description }}

Generated by repave blueprint `terraform-module-resource`.

## Provider scope

- **Cloud provider:** {{ cloud_provider }}
- **Service:** {{ provider_service }}
- **Resource:** {{ provider_resource }}

This thin module scaffolds a single **{{ provider_resource }}** resource under
`{{ provider_service }}` on **{{ cloud_provider }}**. Shared derived values
(tags, naming) live in `locals.tf` per the layout standard.

## Standard

This module targets the repave Terraform standards pack at
`standards/terraform-standards` v{{ _repave_standard_version }}:

- `terraform-standards.md` — engineering standard
- `terraform-module-layout.md` — repository layout and `locals.tf` conventions

The provenance pin is recorded in `repave.yaml` as `{{ _repave_standard_source }}`.

## Usage

```hcl
module "{{ module_name }}" {
  source = "./{{ module_name }}"

  environment = "dev"
  tags = {
    owner = "platform-engineering"
    app   = "example"
  }
}
```

## Inputs

See `variables.tf` for the module input contract.

## Outputs

See `outputs.tf` for module outputs.

## Tests

Run native Terraform tests:

```bash
terraform test
```

Run Checkov (custom policies ship in `policy/checkov/`):

```bash
checkov -d . --config-file .checkov.yml
```

Run the secrets scan (same gate repave runs as `secrets`):

```bash
checkov -d . --framework secrets --enable-secret-scan-all-files
```

## Provenance

Generation metadata is recorded in `repave.yaml` (blueprint, standard, and module
pins). The `provenance-drift` gate validates this file on every generate/publish.
