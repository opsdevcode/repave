# {{ stack_name }} ({{ environment }})

{{ description }}

## Overview

This repository is an **environment stack** for `{{ cloud_provider }}` / `{{ environment }}`.
It composes pinned Terraform modules; provenance is recorded in `repave.yaml`.

Primary module block: `{{ module_name }}` → `{{ module_source }}`{% if module_version %} @ `{{ module_version }}`{% endif %}.

## Layout

- `main.tf` — module composition
- `modules/_example` — replace with your pinned module sources
- `repave.yaml` — repave golden path provenance

## Standard

This stack was generated against repave standard `{{ _repave_standard_source }}`
v{{ _repave_standard_version }}.

## Usage

```hcl
# From a parent root module or after cloning this repository:
terraform init
terraform plan -var="environment=dev"
```

Replace the `{{ module_name }}` module source in `main.tf` with your pinned
`tf-*` / `tfm-*` module coordinates before deploying to shared environments.

## Local development

```bash
terraform init
terraform validate
terraform test
```

The `provenance-drift` gate validates `repave.yaml` on every generate/publish.
