# {{ service_name }} observability

{{ description }}

Generated by repave blueprint `observability-as-code-generic`.

| Field | Value |
| --- | --- |
| Backend | `{{ backend }}` |
| Output mode | `{{ output_mode }}` |
| Environment | `{{ environment }}` |
| Team | `{{ team }}` |
| Organization | `{{ organization }}` |
| Notifications | {{ notification_source }} → {{ notification_target }} |
| Runbook | {{ runbook_url }} |

## Standard

This repository follows `standards/observability/observability-as-code.md` v1.2.0.

## Layout

{% if output_mode == 'terraform' %}
- `versions.tf`, `providers.tf`, `variables.tf`, `monitors.tf` — Datadog monitors via Terraform
{% elif backend == 'prometheus' %}
- `prometheus/rules/` — Prometheus alerting rules (`promtool check rules`)
- `prometheus/alertmanager/` — Alertmanager route/receiver stub (wire `{{ notification_target }}`)
{% elif backend == 'grafana' %}
- `grafana/dashboards/` — Grafana dashboard JSON
{% elif backend == 'datadog' %}
- `datadog/monitors/` — Datadog monitor JSON
{% elif backend == 'otel' %}
- `otel/collector-config.yaml` — OpenTelemetry Collector starter config
{% endif %}

{% if output_mode == 'terraform' %}
## Terraform (Datadog)

Export Datadog credentials before `terraform init` / `plan` / `apply`:

```bash
export DD_API_KEY="<your-api-key>"
export DD_APP_KEY="<your-application-key>"
```

The [Datadog Terraform provider](https://registry.terraform.io/providers/DataDog/datadog/latest/docs)
reads `DD_API_KEY` and `DD_APP_KEY` from the environment. Application keys need
monitor write scope for the generated `datadog_monitor` resources.

Regenerate gates include `terraform-fmt`, `terraform-validate`, and `tflint` when
the Terraform engine is enabled for this artifact type.
{% endif %}

## Usage

{% if output_mode == 'terraform' %}
Run `terraform init`, `terraform plan`, and `terraform apply` from this repository root.
Monitor messages reference **{{ notification_target }}**; wire PagerDuty or Slack in
Datadog monitor notifications after apply.
{% elif backend == 'prometheus' %}
Deploy `prometheus/rules/` to your Prometheus ruler and sync `prometheus/alertmanager/`
via GitOps. Map receivers to **{{ notification_target }}** in your real Alertmanager
config.
{% elif backend == 'grafana' %}
Import JSON from `grafana/dashboards/` via Grafana UI/API or GitOps.
{% elif backend == 'datadog' %}
Import monitor JSON from `datadog/monitors/` via the Datadog API or internal GitOps.
{% elif backend == 'otel' %}
Run or deploy `otel/collector-config.yaml` with the OpenTelemetry Collector distribution
you standardize on; extend exporters beyond `logging` for production.
{% endif %}

## Validate locally

```bash
yamllint .
{% if backend == 'prometheus' and output_mode == 'native' %}
promtool check rules prometheus/rules/*.yaml
{% endif %}
{% if output_mode == 'terraform' %}
terraform fmt -check
terraform validate
{% endif %}
```

## Gates

Repave publish runs blueprint gates where tools are installed (skip cleanly otherwise):

| Gate | When |
| --- | --- |
| `yamllint` | YAML under this repo |
| `promtool` | `prometheus/rules/*.yaml` (Prometheus native) |
| `secrets` | Scans for leaked credentials |
| `docs-drift` | README / standard alignment |
| `provenance-drift` | `repave.yaml` metadata |

Terraform mode adds Terraform engine gates (`terraform-fmt`, `terraform-validate`,
`tflint`, etc.) when configured for the generated artifact.

## Provenance

Generation metadata lives in `repave.yaml`. The `provenance-drift` gate validates it on
every generate/publish.
