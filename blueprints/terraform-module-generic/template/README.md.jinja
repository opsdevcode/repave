# {{ module_name }}

{{ description }}

Generated by repave blueprint `terraform-module-generic`.

## Standard

This module was generated against repave standard `{{ _repave_standard_source }}` v{{ _repave_standard_version }}.

## Usage

```hcl
module "{{ module_name }}" {
  source = "./{{ module_name }}"

  environment = "dev"
  tags = {
    owner = "platform-engineering"
    app   = "example"
  }
}
```

## Inputs

See `variables.tf` for the module input contract.

## Outputs

See `outputs.tf` for module outputs.

## Tests

Run native Terraform tests:

```bash
terraform test
```
