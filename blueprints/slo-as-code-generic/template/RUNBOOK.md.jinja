# {{ service_name }} runbook

{{ description }}

Generated by repave blueprint `slo-as-code-generic`.

## Owner

Team **{{ team }}** ({{ organization }}). Update when ownership transfers.

## Escalation

1. Page **{{ notification_target }}** via {{ notification_source }}.
2. Escalate to platform on-call when burn-rate alerts indicate control-plane impact.
3. Link incident threads to {{ runbook_url }} when using a central wiki.

## Dashboards

- Link golden-signal dashboards for **{{ service_name }}** (availability, latency, errors).
- Include the SLO overview panel fed by `{{ service_name | replace('-', '_') }}:slo:*` recording rules.

## Rollback procedure

1. Roll back the last deployment via GitOps (revert chart/image promotion PR).
2. Confirm `{{ service_name | replace('-', '_') }}:slo:availability:ratio5m` recovers within 15 minutes.
3. Document error-budget consumed in the post-incident review.

## Game-day checklist

- [ ] SLO target ({{ slo_target_percent }}%) agreed with product owner
- [ ] Burn-rate alerts routed to {{ notification_target }}
- [ ] Dashboards show recording rules after deploy
- [ ] Rollback drill completed in the last quarter
