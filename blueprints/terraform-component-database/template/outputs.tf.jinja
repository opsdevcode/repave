output "stack_name" {
  description = "Stack name."
  value       = var.stack_name
}

output "cloud_provider" {
  description = "Cloud provider for this stack."
  value       = local.cloud_provider
}

output "environment" {
  description = "Deployment environment tier."
  value       = var.environment
}

output "primary_module_name" {
  description = "Logical name of the first pinned module block."
  value       = "{{ pinned_modules[0].name }}"
}

output "resource_ids" {
  description = "Resource identifiers from composed modules (for terraform test)."
  value = concat(
{% for mod in pinned_modules %}
    module.{{ mod.name }}.resource_ids{% if not loop.last %},{% endif %}
{% endfor %}
  )
}
