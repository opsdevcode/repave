# {{ service_name }} runbook

{{ description }}

Generated by repave blueprint `app-service-generic`.

## Owner

{{ owner }}

Primary engineering owner for this service. Update when ownership transfers.

## Escalation

1. Page the owning team via your incident tool.
2. Escalate to platform on-call if the service or cluster control plane is impaired.
3. Record timeline and customer impact in the incident channel.

## Dashboards

- Link Grafana or Datadog dashboards for **{{ service_name }}** (golden signals: latency, errors, traffic).
- Link the portal service catalog entry when published.

## Rollback procedure

1. Revert the last merge on `main` or roll back the GitOps promotion PR.
2. Confirm the previous container image tag is live in the cluster.
3. Run smoke tests against `/health` on port {{ port }}.

## Game-day checklist

- [ ] On-call rotation current for the owning team
- [ ] Dashboards load and show recent traffic
- [ ] Alert routes reach the correct notification target
- [ ] Rollback steps exercised in the last quarter
