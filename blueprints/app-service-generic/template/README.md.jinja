# {{ service_name }}

{{ description }}

Generated by repave blueprint `app-service-generic` (runtime: **{{ runtime }}**, layout: **{{ layout }}**).

## Usage

{% if runtime == 'python' -%}
Install dependencies and run tests:

```bash
pip install -e ".[dev]"
pytest
ruff check src tests
```

Run locally:

{% if layout == 'http-api' -%}
```bash
pip install -e .
uvicorn app.main:app --reload --port {{ port }}
```
{% elif layout == 'worker' -%}
```bash
export QUEUE_URL="https://example.com/queues/{{ service_name }}"
python -m app.main
```

Drain gracefully with `SIGTERM` so in-flight messages complete before exit.
{% elif layout == 'scheduled-job' -%}
```bash
python -m app.main
```

Schedule with a Kubernetes `CronJob` (see RUNBOOK.md). Exit code propagates to the job status.
{% elif layout == 'grpc' -%}
```bash
buf lint
buf generate   # after adding buf.gen.yaml and plugins
python -m app.main
```
{% endif -%}
{% elif runtime == 'go' -%}
Run tests and format checks:

```bash
go test ./...
go vet ./...
gofmt -l .
```

Run locally:

```bash
go run ./cmd/server
```
{% elif runtime == 'nodejs' -%}
Install dependencies and run checks:

```bash
npm install
npm run lint
npm test
```

Run locally:

```bash
npm run build
npm start
```
{% elif runtime == 'java' -%}
Run tests and package the service:

```bash
mvn test
mvn package
```

Run locally:

```bash
mvn package
java -jar target/app-{{ service_name }}-0.1.0-SNAPSHOT.jar
```
{% elif runtime == 'dotnet' -%}
Run tests and publish:

```bash
dotnet test tests/App.Tests.csproj
dotnet run --project App.csproj
```

Run locally:

```bash
dotnet run --project App.csproj
```
{% endif %}

Build the container:

```bash
docker build -t {{ service_name }}:local .
```

{% if include_helm_reference == 'true' %}
## Kubernetes / Helm

Deploy using the companion chart repository{% if helm_chart_repo %} (`{{ helm_chart_repo }}`){% endif %}:

```bash
helm upgrade --install {{ service_name }} . --namespace default
```

Replace `helm-{{ service_name }}` with your chart checkout path.
{% endif %}

## Provenance

Lineage is recorded in `repave.yaml` when you publish. Re-run generate to refresh pins.
