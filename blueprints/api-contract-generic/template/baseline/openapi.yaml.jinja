openapi: 3.1.0
info:
  title: {{ api_title }}
  version: "{{ api_version }}"
  description: {{ description }}
paths:
  /health:
    get:
      summary: Liveness
      operationId: getHealth
      responses:
        "200":
          description: OK
