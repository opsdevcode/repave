# {{ spec_name }}

{{ description }}

Generated by repave blueprint `api-contract-generic`.

## Usage

Lint the contract:

```bash
spectral lint --fail-severity=error {% if spec_kind == "asyncapi" %}asyncapi.yaml{% else %}openapi.yaml{% endif %} --ruleset .spectral.yaml
```

{% if spec_kind != "asyncapi" %}
Detect breaking changes versus the published baseline:

```bash
oasdiff breaking baseline/openapi.yaml openapi.yaml
```

When a breaking change is intentional, bump `info.version` and replace
`baseline/openapi.yaml` with the new revision in the same PR.
{% endif %}

## Provenance

Lineage is recorded in `repave.yaml` when you publish. Re-run generate to refresh pins.
