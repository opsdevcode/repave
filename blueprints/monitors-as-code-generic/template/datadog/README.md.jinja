# Datadog monitors

Native Datadog monitor JSON lives under `monitors/`. Import via the
[Datadog monitors API](https://docs.datadoghq.com/api/latest/monitors/) or your
GitOps pipeline.

For Terraform-managed monitors, regenerate with `output_mode=terraform` (see repo
root `README.md`).
