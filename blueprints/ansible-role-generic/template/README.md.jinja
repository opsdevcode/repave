# {{ role_name }}

{{ description }}

Generated by repave blueprint `ansible-role-generic`.

## Role metadata

- **Galaxy namespace:** {{ namespace }}
- **Role name:** {{ role_name }}
- **Minimum Ansible version:** {{ min_ansible_version }}
- **Target platforms:** {{ target_platforms }}

This role follows the standard Ansible layout (`tasks/`, `defaults/`, `handlers/`,
`vars/`, `meta/main.yml`) and ships with a Molecule scenario under
`molecule/default/`.

## Standard

This role was generated against repave standard `{{ _repave_standard_source }}`
v{{ _repave_standard_version }}.

## Usage

```yaml
- hosts: webservers
  roles:
    - role: {{ namespace }}.{{ role_name }}
```

Install from Galaxy (after publish):

```bash
ansible-galaxy role install {{ namespace }}.{{ role_name }}
```

## Development

Run lint and syntax gates locally (same tools repave uses when installed):

```bash
yamllint .
ansible-lint
ansible-playbook --syntax-check molecule/default/converge.yml
molecule test
```

## Provenance

Generation metadata is recorded in `repave.yaml` (blueprint, standard, and role
pins). The `provenance-drift` gate validates this file on every generate/publish.
